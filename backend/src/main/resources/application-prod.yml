spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  cache:
    redis:
      time-to-live: 7200000

chatbot:
  context-size: 20
  max-tokens: 2000
  truncate-limit: 5000
